{
  "name": "stable-diffusion-webui",
  "version": "1.0.0",
  "description": "Stable Diffusion web UI",
  "scripts": {
    "preinstall": "wget -q https://raw.githubusercontent.com/AUTOMATIC1111/stable-diffusion-webui/master/webui.sh",
    "start": "./webui.sh --skip-torch-cuda-test --precision full --no-half --use-cpu all"
  },
  "dependencies": {
    "hop": "^1.0.0"
  }
}
